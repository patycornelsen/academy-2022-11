version: 2

models:
  - name: dim_funcionarios
    description: Tabela dimensão de funcionários.
    columns:
      - name: sk_funcionario
        description: Chave primária da tabela de funcionários.
        tests:
          - unique
          - not_null