version: 2

models:
  - name: fct_vendas
    description: Tabela fatos com dados de venda para cada item de um pedido como valor bruto, valor liquido e total de desconto.
    columns:
      - name: sk_vendas
        description: Chave primária da tabela de vendas.
        tests:
          - unique
          - not_null